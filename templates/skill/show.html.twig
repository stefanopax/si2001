{% extends 'base.html.twig' %}

{% block title %}Skill{% endblock %}

{% block body %}
    <h1>Skill</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Name</th>
                <td>{{ skill.name }}</td>
            </tr>
        </tbody>
    </table>
    <div class="d-flex justify-content-start">
        <a href="{{ path('skill_index') }}" class="btn btn-info">Back to list</a>
        &nbsp
        <a href="{{ path('skill_edit', {'id': skill.id}) }}" class="btn btn-secondary">Edit</a>
        &nbsp
        {{ include('skill/_delete_form.html.twig') }}
    </div>
{% endblock %}
